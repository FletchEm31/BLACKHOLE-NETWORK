import requests

def fetch_meteoblue_forecast(url, api_key, lat, lon):
    params = {
        "apikey": api_key,
        "lat": lat,
        "lon": lon,
    }
    r = requests.get(url, params=params, timeout=20)
    r.raise_for_status()
    return r.json()