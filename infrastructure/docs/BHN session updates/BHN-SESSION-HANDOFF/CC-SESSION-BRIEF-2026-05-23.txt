CC SESSION BRIEF — 2026-05-23
================================================================================

SESSION GOAL: Three workstreams. Read all referenced project files before
touching anything.

================================================================================
WORKSTREAM 1 — FRANKFURT DECOMMISSION (FINAL CLEANUP)
================================================================================

Frankfurt (EH|VPS-FRANKFURT-EU1, 192.248.187.208) was decommissioned May 2026.
Archive folder exists at infrastructure/archive/frankfurt/. Physical VPS is
still billing at ~$12/month. Complete the decommission now.

STEP 1 — Verify nothing depends on Frankfurt anymore. From PC LA CONSOLE:

ssh root@10.8.0.1 'wg show wg0'

Should NOT show 10.9.0.2 as an active peer. If it does, remove the Frankfurt
peer block from /etc/wireguard/wg0.conf and reload wg0.

STEP 2 — Move stale docs to the archive folder. These files still reference
Frankfurt and need to move to infrastructure/archive/frankfurt/:

infrastructure/docs/bhn-frankfurt-exit-routing.md
infrastructure/docs/bhn-frankfurt-scoping.md
infrastructure/docs/frankfurt-exit-backlog.md

Update infrastructure/docs/bhn-network-data-flow.md — strip all FRA-specific
traffic classes, keep current topology only (LA hub, NJ, Hillsboro).

STEP 3 — Commit the archive cleanup.

Summary: chore(infra): complete Frankfurt decommission — archive stale docs

Description: Moves FRA-specific docs to infrastructure/archive/frankfurt/.
Strips FRA references from bhn-network-data-flow.md. Frankfurt VPS
(192.248.187.208) physically cancelled by operator. Tor relay BHNFornaxEU1
retired. MyFamily now: BHNHeliosUS3 (Hillsboro) + BHNNebulaUS2 (NJ) only.

STEP 4 — FLAG FOR OPERATOR: Log into Vultr and destroy the Frankfurt VPS
manually. CC cannot do this. State clearly in session output when the commit
is done so operator knows to go cancel the VPS.


================================================================================
WORKSTREAM 2 — NEW N8N EBAY WORKFLOWS: PSA + CGC
================================================================================

We have existing eBay comps workflows for BGS and SGC sets. Now build the
equivalent for PSA and CGC using the same proven pattern.

BEFORE WRITING ANY CODE, READ THESE FILES:

infrastructure/docs/pokemonbhn/collectibles-data-standard.md  (THE binding authority)
README.md  (PokemonBHN section — architecture and table relationships)
scripts/operator-pc/clone-vintage-workflow.js  (existing workflow clone pattern)
All existing JSON files in n8n-workflows/  (match the proven pattern exactly)


NON-NEGOTIABLE RULES FROM THE DATA STANDARD:

grade = verbatim raw_label TEXT. Must exist in master_grade_catalog(grader,
raw_label) before insert. Unknown grades go to grade_reject_log — never
silently dropped or silently accepted.

grader = codes only: CGC, PSA, BGS, SGC. Never full descriptors.

edition values: 1st Edition, Unlimited, Shadowless, N/A. CHECK-bounded. NOT NULL.

print_variant values: Standard, Holo, Error, No Symbol, W Stamp, Winner, Jumbo,
Prerelease, Gold Border, Red Cheeks, WB Movie, Nintendo Power, WOTC,
1999-2000 Copyright. Default is Standard. NOT NULL.

listed_price and sold_price are separate columns. NULL means absent. 0 means free.

card_number = bare integer, no # prefix.

Soft validate on ebay_listings (live high-churn feed) — no hard FK, but
validate-and-log. Hard FK on pop_reports and sold_listings.


PSA-SPECIFIC NOTE:

PSA heading mapping lives in infrastructure/scrapers/psa-sets.json. PSA slugs
are not derivable from set names. 7 of 8 sets are mapped. Wizards Black Star
Promos is the exception — skip it entirely (flagged for multi-heading support,
not built yet).


WHAT TO BUILD:

1. n8n workflow: PSA eBay sold comps — matching structure of existing workflows
2. n8n workflow: CGC eBay sold comps — matching structure of existing workflows
3. Export both as JSON to n8n-workflows/ with clear naming convention
4. Verify both write to sold_listings with full standard compliance


================================================================================
WORKSTREAM 3 — GYM HEROES + GYM CHALLENGE: SCHEMA + N8N WORKFLOWS
================================================================================

Add Gym Heroes and Gym Challenge to the PokemonBHN catalog and build their
complete data collection pipeline.

BEFORE BUILDING ANYTHING, READ THE SAME FILES AS WORKSTREAM 2. Then also run:

sudo -u postgres psql -d eventhorizon -c "SELECT set_name, release_year, era, total_cards, legal_editions, psa_headings FROM master_set_catalog ORDER BY release_year;"

sudo -u postgres psql -d eventhorizon -c "SELECT COUNT(*), set_name FROM master_card_catalog WHERE set_name ILIKE '%gym%' GROUP BY set_name;"


NON-NEGOTIABLE CHANGE CONTROL RULE (standard section 8):

New set_name, edition, print_variant, grade label, or grader must be added to
the relevant master_* authority table FIRST before any fact row can reference
it. The FK and CHECK constraints enforce this. Do not skip this step.


STEP 1 — Add to master_set_catalog:

Gym Heroes: WOTC era, 1st Edition + Unlimited, ~132 cards
Gym Challenge: WOTC era, 1st Edition + Unlimited, ~132 cards
Verify exact PSA headings from psa-sets.json before inserting.
Use the exact same column structure as the existing 8 sets.


STEP 2 — Add cards to master_card_catalog:

Insert all Gym Heroes and Gym Challenge cards.
active = true to enroll them across all collectors.
edition and print_variant populated per standard.
card_number = bare integer, no # prefix.


STEP 3 — Build n8n workflows for both sets:

eBay sold comps workflow for Gym Heroes (all graders)
eBay sold comps workflow for Gym Challenge (all graders)
Pop reports workflow for both (CGC live; PSA per psa-sets.json mapping)
All workflows fully standard-compliant — same rules as Workstream 2.
Export all workflow JSON files to n8n-workflows/.


STEP 4 — Commit everything.

Summary: feat(pokemonbhn): add Gym Heroes + Gym Challenge — schema, catalog, n8n workflows

Description: Full list of tables touched, workflows added, PSA heading mappings
used. Note any open items such as if SGC/BGS pop reports are not yet built for
these sets.


================================================================================
GENERAL RULES FOR THIS SESSION
================================================================================

Do NOT touch: n8n (port 5678), Grafana (3000), PostgreSQL core config,
WireGuard mesh, trading stack, fail2ban, CrowdSec, Suricata.

Always check collectibles-data-standard.md before any schema decision. It is
the binding authority, not intuition.

The live database beats the docs where they conflict. Query the DB to verify
current state before assuming anything.

Reject path is mandatory. Any workflow writing grades must handle unknown
labels via grade_reject_log. Never silent drop.

Commit each workstream separately. Do not bundle Frankfurt cleanup with schema
additions.

Check RAM on LA before any Docker operations: free -h && docker stats --no-stream

================================================================================


================================================================================
WORKSTREAM 4 — DEPLOY REDIS FOR HORIZON SHORT-TERM MEMORY
================================================================================

Redis is HORIZON's hot working-memory layer (short-term session memory).
The deploy doc already exists in the repo at:
infrastructure/services/redis/docker-run.md

Read that file in full before doing anything. It has the exact Docker command,
env file path, UFW notes, and n8n credential setup.

STEP 1 — Check if Redis is already running on LA.

ssh root@10.8.0.1 'docker ps | grep redis'
ssh root@10.8.0.1 'redis-cli -h 10.8.0.1 ping 2>/dev/null || echo "not running"'

If it is already running and healthy, document that and skip to STEP 4 to
verify the n8n credential is wired.

STEP 2 — If Redis is NOT running, deploy it per the docker-run.md doc.

Key parameters from the doc:
Image: redis:7-alpine
Port: 6379 (VPN-only, bound to 10.8.0.1)
Password: pull from Proton Pass entry EH-Redis-Password (operator must supply)
Maxmemory: 128mb with LRU eviction
NO persistence — no AOF, no RDB (Redis is a cache; PG is source of truth)
Compose file lives at: /opt/bhn-horizon-redis/docker-compose.yml

Check RAM before deploying. Redis footprint is ~30MB idle.
free -h && docker stats --no-stream

STEP 3 — Apply the conversation_sessions schema to PostgreSQL.

sudo -u postgres psql -d eventhorizon -f sql/redis-memory-schema.sql

Verify the table was created:
sudo -u postgres psql -d eventhorizon -c "\d conversation_sessions"

STEP 4 — Verify UFW allows Redis on the WireGuard interface.

ssh root@10.8.0.1 'ufw status verbose | grep -E "(wg0|6379)"'

The existing wg0 ACCEPT rule should cover it. If there is an explicit DENY
for 6379, remove it. No new rules should be needed.

STEP 5 — Flag for operator: add the Redis credential in n8n manually.

CC cannot access the n8n UI directly. Flag this clearly in session output.
The operator needs to add a new n8n credential with these values:
  Name: EH HORIZON Redis (LA)
  Type: Redis
  Host: 10.8.0.1
  Port: 6379
  Password: (from Proton Pass EH-Redis-Password)
  Database: 0
  SSL: off

Then test the credential in n8n — should return "Connection successful".

STEP 6 — Verify Redis is healthy after deploy.

ssh root@10.8.0.1 'docker exec bhn-horizon-redis redis-cli -a "$REDIS_PASSWORD" ping'
Should return PONG.

ssh root@10.8.0.1 'docker exec bhn-horizon-redis redis-cli -a "$REDIS_PASSWORD" INFO memory | grep used_memory_human'

Document the result and confirm Redis is live before closing this workstream.



================================================================================
WORKSTREAM 5 — HORIZON SYSTEM STATE UPDATE + WORKFLOW RE-IMPORT
================================================================================

CRITICAL CONTEXT: HORIZON is currently running a STALE workflow. It still
identifies itself as "Nathan" and cannot see financial or trading tables.
This has been the top blocker for HORIZON functionality. Before touching
anything, read the following files in full:

infrastructure/docs/horizon-roadmap.md
infrastructure/docs/bhn-horizon-workflow-handoff.md
n8n-workflows/bhn-horizon.json
scripts/horizon-fix.sh

WHAT HAS CHANGED SINCE HORIZON WAS LAST UPDATED (full system state):

Platform is now called BLACKHOLE NETWORK (BHN) — never EventHorizon.

Node inventory (current):
  LA hub          149.28.91.100   10.8.0.1    Hub, PG, n8n, Grafana
  New Jersey      140.82.4.35     10.8.0.5    Trading node (port 2222)
  Hillsboro       5.78.94.237     10.8.0.6    Egress proxy (tinyproxy)
  Frankfurt       DECOMMISSIONED  — offline and being destroyed this session

Database: eventhorizon on LA — now 78+ tables. agent_reader has SELECT on all.

Major table additions since HORIZON's stale workflow was built:
  All financial intelligence tables (market_daily, macro_daily, market_regimes,
  market_sentiment, market_events, earnings_data, analyst_data, energy_prices,
  agriculture_prices, alpaca_news, investment_signals, v_ticker_analysis view)
  All trading tables (paper_trades, signals_log, trading_strategies,
  circuit_breaker_log, strategy_performance, reconciliation_heartbeat)
  All security/infra monitoring tables (wg_peer_stats, node_resource_stats,
  node_bandwidth_stats, node_disk_stats, container_stats, security_events,
  pulse_reports, crowdsec_decisions, fail2ban_events, node_logs_summary)
  All collectibles tables (master_card_catalog, master_grade_catalog,
  master_set_catalog, master_grading_criteria_catalog, pop_reports,
  sold_listings, ebay_listings)
  HORIZON memory tables (memories with pgvector 384-dim, conversation_sessions,
  agent_token_log, call_transcripts, qa_cache)

All tables above have GRANT SELECT TO agent_reader already applied.
Run the verification script to confirm:
sudo -u postgres psql -d eventhorizon -f sql/horizon-agent-reader-grants-verify.sql

Trading stack status (HORIZON must know this):
  Only strat_13_rsi_intraday is active. All other strategies paused.
  This is by operator decision — single-strategy validation mode.
  Three Alpaca paper accounts: PA39LSUT2NW8 (PRIMARY $100k),
  PA3AZX0UE3JC (FUNDAMENTAL $25k), PA37PRN150AG (SIGNALS $25k).
  ALPACA_BASE_URL = https://paper-api.alpaca.markets (no /v2 suffix).

Current phase status (HORIZON must know this):
  Phase 1 Network: ~90% complete
  Phase 2 Dashboard: ~85% complete (6 Grafana dashboards live)
  Phase 3 AI: ~60% complete (HORIZON operational but stale)
  Phase 4 Per-Node: ~80% complete
  Phase 5 Resilience: ~10% complete

Twilio A2P 10DLC approval was pending as of May 23 — SMS may now be live.
ElevenLabs Charlotte voice is on Creator plan.
HORIZON number: +1 310 929 6201


STEP 1 — Re-import bhn-horizon.json into n8n.

This requires a browser session on WireGuard VPN to n8n at http://10.8.0.1:5678
CC cannot do this directly via the n8n UI. However CC can:

a) Verify the current workflow state via the n8n SQLite database:
ssh root@10.8.0.1 'sqlite3 /root/.n8n/database.sqlite "SELECT id, name, active FROM workflow_entity WHERE name LIKE '\''%HORIZON%'\'' OR name LIKE '\''%horizon%'\'';"'

b) Check if the horizon-fix.sh script can be run to fix the publish state:
ssh root@10.8.0.1 'cat /root/scripts/horizon-fix.sh 2>/dev/null || cat /opt/bhn/scripts/horizon-fix.sh 2>/dev/null'

If the fix script exists and the workflow just needs activeVersionId corrected,
CC can run it:
ssh root@10.8.0.1 'bash /path/to/horizon-fix.sh'

c) If a full re-import is needed (workflow is genuinely stale and needs
replacement), flag this for the operator to do manually via the n8n UI.
The operator must: open n8n, import bhn-horizon.json, rewire the Postgres
EventHorizon and EH-Twilio credentials, activate the workflow.
Do NOT refresh the n8n editor during this process.


STEP 2 — Verify agent_reader grants cover all new tables.

sudo -u postgres psql -d eventhorizon -f sql/horizon-agent-reader-grants-verify.sql

Check the output for any "skipping" entries on tables that DO exist.
If any existing table was skipped, manually grant:
sudo -u postgres psql -d eventhorizon -c "GRANT SELECT ON <tablename> TO agent_reader;"


STEP 3 — Add SMTP credentials to env on LA for HORIZON daily summary email.

Check if SMTP is already configured:
ssh root@10.8.0.1 'grep -i smtp /etc/bhn-trading/env 2>/dev/null | sed "s/=.*/=REDACTED/"'

If SMTP credentials are missing, flag for operator to add manually.
These should be in Proton Pass. The from address is horizon@eventhorizonvpn.com.
Do not add credentials yourself — flag and let operator supply them.


STEP 4 — Verify HORIZON can reach the database after workflow update.

If the workflow was successfully updated or fixed, test via the chat webhook:
curl -s -X POST http://10.8.0.1:5678/webhook/ec1592c6-8715-4b0f-8ee8-5bc02f551a27/chat \
  -H 'Content-Type: application/json' \
  -d '{"chatInput":"What is the current network status and how many nodes are online?","sessionId":"cc-test-001"}'

A healthy response should reference actual node data from the database,
not a generic reply. Document the full response.


STEP 5 — Produce a HORIZON status summary.

Cover:
  - Is the workflow current or still stale? (Nathan vs HORIZON identity)
  - Can HORIZON see financial tables? Test with a market query.
  - Can HORIZON see trading tables? Test with a trading query.
  - Is Redis running and connected to n8n?
  - Is pgvector memory working? (Check memories table row count)
  - What is still blocked? (SMTP, voice webhook, n8n re-import if needed)

Flag any item that requires operator manual action in the n8n UI clearly
so operator knows exactly what to do and in what order.

================================================================================
