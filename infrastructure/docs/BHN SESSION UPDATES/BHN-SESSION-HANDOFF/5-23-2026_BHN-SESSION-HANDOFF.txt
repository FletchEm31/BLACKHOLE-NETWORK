================================================================================
BHN SESSION HANDOFF
Session date: 2026-05-23 (Saturday)
Operator: Fletch / Hayden Harper | GitHub: FletchEm31
================================================================================

This handoff supersedes prior. Read this FIRST next session. Save to:
D:\GITHUB REPOSITORY\BLACKHOLE-NETWORK\infrastructure\docs\BHN session updates\BHN-SESSION-HANDOFF\


================================================================================
🚨 CRITICAL — DO BEFORE MONDAY MARKET OPEN (13:30 UTC / 6:30 AM PST)
================================================================================
STRAT 13 IS ARMED TO TRADE MONDAY, UNVALIDATED, WITH KILLSWITCH BYPASSED.

CC2 confirmed (FIN-006):
- strat_13_rsi_intraday shows status='active' AND halted=true, BUT should_run()
  IGNORES the halted boolean — so the killswitch is effectively OFF. Strat 13
  WILL trade Monday 13:30 UTC.
- Strat 13 is MISSING from rules_schema.py STRATEGY_SCHEMAS (only strat_1-8
  defined). validate_rules.py silently skips it across all 4 validation layers.
  It is running with ZERO field-level validation on RSI thresholds, broker key,
  ticker, or any parameter.

ACTION (pick one before market open):
  a) Properly halt Strat 13, OR
  b) Fix should_run() to honor the halted boolean, OR
  c) Add strat_13_rsi_intraday to rules_schema.py + STRAT_NUMBER/SMS_NAME_MAP
     in trading_core.py so it validates.
Do NOT let it trade unvalidated with a non-functional killswitch.


================================================================================
WHAT GOT DONE THIS SESSION
================================================================================

FRANKFURT DECOMMISSION (CC1 + CC2)
- All mesh-side FRA peers / BHNFornax / Tor refs removed from LA wg0, NJ,
  Hillsboro. Frankfurt docs archived to infrastructure/archive/frankfurt/.
- 3 commits pushed to BLACKHOLE-NETWORK earlier (parallel-cc rules, Frankfurt
  archive, agent_reader grants backfill).
- STILL PARKED: the leftover FRA egress peer on LA wg0 (AllowedIPs=0.0.0.0/0,
  192.248.187.208) + table-100 routing. Removal draft written & approved
  (Option A — migrate client 10.8.0.8 to Hillsboro). DO NOT EXECUTE until the
  Vultr VPS is destroyed Monday. Draft at:
  infrastructure/docs/.../CC2-FRA-REMOVAL-DRAFT-PARKED-2026-05-23.md

DNS PRIVACY FIX (CC1) — COMMITTED b137c21
- dnscrypt-proxy query_log DISABLED. query.log + rotated .gz wiped.
- dns_query_log PG table TRUNCATED (8,788 rows of client-IP+qname privacy debt
  → 0). Broken bhn-dns-log-collector cron archived.
- NOTE: This is the PRIVACY LOGGING fix. It is SEPARATE from the traffic/data-
  flow (Frankfurt routing) item, which is still parked (see above).
- Outstanding sub-item: nx.log at /var/log/dnscrypt-proxy/nx.log (158 KiB of
  NXDOMAIN qnames) still being written — not addressed this round.

HORIZON (CC1) — COMMITTED ccf495e
- Confirmed running CURRENT workflow D2groJaxtihbfV3f (not stale "Nathan" one).
- Identity correct, 3-node topology (Frankfurt decommissioned), 7 agent_reader
  grants live and exercised.
- System prompt extended with MEMORY ARCHITECTURE section (Redis hot tier +
  conversation_sessions durable + pgvector semantic).
- n8n-workflows/bhn-horizon.json resynced in repo, READY for operator re-import.
- BTEH-flagged but NOT yet fixed: token logging audit trail (SEC-003/AGENT-003),
  per-tool rate/cost caps (AGENT-001).

BLACKBOXBIDDER (CC3) — REPO LIVE ON GITHUB
- FletchEm31/BLACKBOX-BIDDER created (private) + pushed via HTTPS. Root commit
  633575f, 17 files, 1,401 insertions. Pushed clean (30 objects).
  Local: D:\GITHUB REPOSITORY\BLACKBOX-BIDDER
- Phase 1 scaffold only: src/{collectors,fmv,scoring,sellers,dashboard} stubs,
  sql/ schema design, sql/proposed-bhn-changes/ (CC1-review diffs), config/,
  docs/{design.md, build-plan.md, brief/}.
- Guardrails HONORED: no live eBay calls, no SQL applied to eventhorizon, no
  code in src/, no billing/auth/sniping, read-only on BLACKHOLE-NETWORK.
- Memory saved (project_blackbox_bidder.md) so future CC inherits Phase 1
  gating + read-only-on-BHN constraint.

BTEH AUDIT (App Claude)
- Full BTEH v1.0 README read end-to-end (1,356 lines). It is a genuine
  professional audit (AWS WA, CIS, NIST CSF/AI RMF, OWASP ASVS+LLM Top 10,
  STRIDE, CVSS v4.0, DAMA-DMBOK). Has its own multi-instance run model in
  Appendix B (4 analysis instances + synthesis, run in Project tabs — NOT CC).
- Finding division written, mapping still-open BTEH findings to CC lanes:
  BTEH-CC-Remediation-Division-2026-05-23.txt
- Several v1.0 findings now STALE/RESOLVED: Frankfurt (INFRA-002, SEC-006),
  dns_query_log (now truncated), HORIZON identity, Redis deployed.

AUDIT FINDINGS (CC1) — REASSURING
- Backups/disk are HEALTHY (this contradicted the earlier worry — not a crisis):
  root 39%, NVMe hot tier 1%, HDD 1%. PG db only 106 MB. eh-purge deployed,
  cron'd every 2 days, last 9 runs all success. 80% threshold untested (never
  reached). Restore test still recommended (INFRA-007) but no capacity issue.


================================================================================
OPERATOR ACTIONS — MANUAL, CARRY TO MONDAY
================================================================================
1. STRAT 13 — fix/halt before 13:30 UTC Monday (see CRITICAL section above).
2. VULTR DESTROY — EH|VPS-FRANKFURT-EU1 (192.248.187.208). Locked out until
   Monday. Kills $12/mo. AFTER destroy → unpark CC2's FRA removal draft.
3. EBAY LETTER — eBay-Commercial-Disclosure-Letter.txt is DRAFTED, not sent.
   Edit to your voice, send via eBay Developer support. Phase 1 = personal use
   only; data protected, shared with no one not approved by eBay. Do NOT form
   LLC or take commercial action until eBay replies on the commercial path.
4. PUSH CC1 COMMITS — b137c21 + ccf495e (tell CC1 "push" if not already done).
5. n8n REDIS CREDENTIAL — add in n8n UI: "EH HORIZON Redis (LA)", Redis type,
   host 10.8.0.1 port 6379, password from Proton Pass EH-Redis-Password, DB 0,
   SSL off. Required before HORIZON short-term memory works.
6. HORIZON RE-IMPORT — import n8n-workflows/bhn-horizon.json via n8n UI when
   ready. ⚠️ Do NOT run scripts/horizon-fix.sh (targets old inactive workflow
   fTFjaf2Q2aQrOPsY, would resurrect stale "Nathan" workflow).
7. WIREGUARD KEY ROTATION — the wg0.conf dump exposed the LA hub PrivateKey +
   all peer PSKs in terminal scrollback. No observable compromise, but rotate
   when convenient. Backlog, not emergency.
8. CC3 — answer the 7 Stage-1 design questions (in docs/build-plan.md):
   (1) DB role: new bbb_writer vs reuse ehuser [rec: new]
   (2) Vertical scope: WOTC 8 sets only, or extend day 1
   (3) /fmv stack: Node vs Python
   (4) Scheduling: cron on LA vs new n8n workflow
   (5) Backtest source for tuning weights
   (6) Dashboard auth model [rec: none — WG perimeter]
   (7) drop BHN-Ecosystem-Overview.txt into the BBB repo (CC3 never got it)
9. ADD BTEH README to this Claude project's knowledge (only in uploads now) so
   future sessions work from the real audit, not a description.


================================================================================
DATA PIPELINE FINDINGS (CC2) — DEDICATED SESSION NEEDED
================================================================================
Dead/stale collectors, single root cause dated ~May 13 ("May-13 dead cluster"):
- crypto_market_data, prediction_market_data, alpaca_news: cron firing, ZERO
  output — broken collector CODE (not target-registry), likely auth/connection.
- agriculture_prices dead since January; energy/earnings/analyst stale 10-19 days.
- Maps to BTEH PIPE-003 (silent failure, no dead-letter) + PIPE-004 (no data
  quality assertions).
Next-session fix: tail cron logs (journalctl -t CRON --since 2026-05-13 |
grep bhn-resource-collector), find failure mode, add to table_freshness_targets.


================================================================================
OTHER OPEN BTEH ITEMS (verify/remediate future sessions)
================================================================================
- SEC-002/AGENT-006: n8n encryption key backup to Proton Pass (separate from DB);
  rotate Anthropic key quarterly + set usage cap.
- INFRA-006: LA egress lockdown (ufw-rewrite.sh lockdown) never run — LA can
  still make direct 443/587 egress bypassing Hillsboro tinyproxy.
- DB-006: Redis hardening — confirm bound 127.0.0.1/VPN only + requirepass set.
- INFRA-004/SEC-001: CVE-2026-31431 mitigated (algif_aead blacklisted) but full
  kernel patch+reboot pending. NJ first (holds Alpaca creds), needs trading halt.
- FIN-001/003/004: confirm true NJ trading-stack deployment state (systemd units,
  rules.json, env).
- PIPE-002/LEGAL-002: confirm eBay sold-comps pipeline mechanism (API vs scrape)
  — directly relevant to BBB.
- Postgres roles: ehuser unknown scope (DB), n8n_user scope review (DB-009),
  previously-exposed ehuser/log_shipper creds — rotation status unknown.


================================================================================
CURRENT NODE STATUS
================================================================================
LA Hub      <BHN_LA_PUBLIC_IP> / 10.8.0.1  — PostgreSQL eventhorizon, n8n, HORIZON,
            Grafana, Redis, Docker. Healthy. DNS logging now OFF.
NJ          <BHN_NJ_PUBLIC_IP> / 10.8.0.5 (SSH port 2222) — Alpaca paper trading.
            Strat 13 armed (SEE CRITICAL).
Hillsboro   <BHN_HIL_PUBLIC_IP> / 10.8.0.6 — tinyproxy egress, Tor relay BHNHeliosUS3.
Frankfurt   DECOMMISSIONED. VPS destroy pending Monday. wg0 egress peer parked.


================================================================================
DELIVERABLES PRODUCED THIS SESSION (save all to handoff folder)
================================================================================
- BHN-SESSION-HANDOFF.txt (this file)
- BTEH-CC-Remediation-Division-2026-05-23.txt
- BlackboxBidder-Analysis-and-Scope.txt (incl. tiers/pricing/data layer)
- BHN-Ecosystem-Overview.txt
- eBay-Commercial-Disclosure-Letter.txt (DRAFT — edit + send)
- CC3-SESSION-BRIEF-2026-05-23.txt
Plus committed in repos:
- CC2-AUDIT-FINDINGS-2026-05-23.md (handoff folder, by CC2)
- CC2-FRA-REMOVAL-DRAFT-PARKED-2026-05-23.md (handoff folder, by CC2)
- infrastructure/docs/security/dns-query-logging-disabled-2026-05-23.md (CC1)
- infrastructure/docs/parallel-cc-execution-rules.md (CC1)

================================================================================
END HANDOFF
================================================================================


================================================================================
⚠️ UNVERIFIED — DISPATCHED BUT NOT CONFIRMED THIS SESSION (check next session)
================================================================================
These were assigned to CC2 but the loop did NOT close before session end. Do
NOT treat as "checked." Confirm before relying on any of it:

TRADING / NJ:
- NJ node health + true trading-stack deployment state (BTEH FIN-001/003/004).
  Open contradiction: if systemd units/rules.json/env are NOT fully deployed on
  NJ, how is Strat 13 "active"? Resolve this. Confirm NJ worker alive + Alpaca
  connection live + SOMETHING actually executing.
- Strats 1-8 status — not checked this session. Unknown.
- Strat 13 shares SIGNALS Alpaca account (PA37PRN150AG) with Strat 4 —
  contaminates clean validation. Flagged earlier, never resolved. Move Strat 13
  to its own paper account.
- master_killswitch integrity (FIN-008) — given should_run() ignores halted,
  verify the killswitch actually halts everything, not just Strat 13.

FINANCIAL / SCRAPER DATA:
- CONFIRMED dead (CC2): crypto_market_data, prediction_market_data, alpaca_news
  (cron firing, zero output, broken collector CODE since ~May 13);
  agriculture_prices dead since Jan; energy/earnings/analyst stale 10-19 days.
  Needs a dedicated fix session — tail cron logs from May 13, find failure mode.
- eBay sold-comps pipeline mechanism (API vs scrape) — UNVERIFIED (BTEH
  PIPE-002). Directly relevant to BBB. Confirm which eBay API/endpoint, auth,
  rate-limit compliance.

n8n WORKFLOWS — CURRENT STATE:
- HORIZON workflow (D2groJaxtihbfV3f) is the live active one; identity correct,
  grants live, system prompt updated + resynced to repo (commit ccf495e).
  STILL NEEDS: operator re-import of n8n-workflows/bhn-horizon.json via n8n UI,
  AND the Redis credential added in n8n UI, before short-term memory works.
- Old inactive workflow fTFjaf2Q2aQrOPsY still present — leave alone. Do NOT run
  scripts/horizon-fix.sh (it targets the old one).
- Other n8n workflows (PSA/CGC eBay scrapers, financial collectors) — these are
  the n8n side of the dead-collector cluster above. Their workflow health was
  NOT individually audited this session. Verify each writes successfully and
  uses upsert (BTEH PIPE-007 idempotency) when the pipeline fix session happens.
- Full n8n workflow inventory + per-workflow last-success audit = part of the
  BTEH Section 4 (Pipeline) re-audit. Defer to the full audit.

PLAN: Run the FULL BTEH re-audit (Appendix B: 4 analysis instances A/B/C/D +
SYNTHESIS, in Claude.ai Project tabs — NOT Claude Code) AFTER BlackboxBidder
ships and AFTER the CCs remediate live state, so v1.1 reflects reality not the
stale v1.0 baseline. The trading + financial + n8n items above feed Instance B
(DB+Pipeline) and Instance C (Agent+Code+Financial).
================================================================================


================================================================================
WS3 CORRECTION — GYM HEROES / GYM CHALLENGE (accurate status)
================================================================================
CC1 originally marked WS3 "N/A — already done." That was WRONG. The truth:
- 1st Edition Gym Heroes + Gym Challenge ARE covered (pre-existing nodes in the
  4 vintage workflows: bgs/cgc/psa/sgc). CC1 did NOT build these — they already
  existed.
- UNLIMITED edition Gym Heroes + Gym Challenge are NOT collected at all. Never
  built. This is the actual WS3 work, still pending. We are only capturing half
  the market for these two sets.
- Minor: node labels say "Gym Heros" (typo, missing 'e') in all 4 files.
  Cosmetic — search strings use "heroes" correctly so searches work.

PLAN: Build the Unlimited versions next session (~3 hrs out). It's a
duplicate-and-modify of the existing 1st Edition nodes — same sets, same
graders, same structure, just Unlimited. 8 new search nodes (2 sets x 4
graders). Full step-by-step in: WS3-Unlimited-Gym-Gameplan.txt
DONE = both 1st Edition AND Unlimited collected across all 4 graders.
================================================================================
