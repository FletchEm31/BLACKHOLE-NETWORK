# BHN Tor relay — NJ trading node only

# NJ's WG tunnel IP (wg2). NOTE: NJ tunnel is currently broken
# (Vultr cross-region block suspected, parked 2026-05-11). The SocksPort
# binding will be unreachable until the tunnel is fixed, but the relay's
# primary ORPort function (public 9001/tcp) works independently of the
# tunnel.
BIND_IP=10.10.0.2

# Identity, bandwidth, accounting are HARDCODED in torrc:
#   Nickname     BHNNewJersey
#   ContactInfo  admin@eventhorizonvpn.com
#   RelayBandwidthRate  512 KB   (half of Frankfurt's — trading-node coexistence)
#   AccountingMax       750 GB   (half of Frankfurt's)
