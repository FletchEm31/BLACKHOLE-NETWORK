# BHN SearXNG — Frankfurt only

# Frankfurt's WG tunnel IP (wg1 interface)
BIND_IP=10.9.0.2

# Secret key for session signing.
# Generate ONCE per node with:
#   openssl rand -hex 32
# Then paste the output here. Do NOT commit the resulting .env file
# (it's gitignored by the repo's .gitignore).
SEARXNG_SECRET=
