node_modules/
package-lock.json

# Transient scrape output. Regenerated each run on operator-pc and in /var/lib/bhn-cgc-pop/ on LA.
*-1st-edition.json

# PSA scraper transients: stealth browser profile (holds cf_clearance), calibration captures,
# test output, and per-set JSON output. Keep the curated set map (psa-sets.json).
_psa-profile/
_psa-probe-out/
_psa-test/
_psa_*.html
psa-*.json
!psa-sets.json
